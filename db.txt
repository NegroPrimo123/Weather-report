CREATE TABLE cached_weather (
    id SERIAL PRIMARY KEY,
    city VARCHAR(100) NOT NULL,
    country VARCHAR(100),
    data JSONB NOT NULL,
    timestamp TIMESTAMP DEFAULT NOW(),
    expires_at TIMESTAMP NOT NULL
);

CREATE INDEX idx_cached_weather_city ON cached_weather(city);
CREATE INDEX idx_cached_weather_expires ON cached_weather(expires_at);